# ============================================================
# HistoryVidGen Environment Configuration
# ============================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# All client-side environment variables MUST use the VITE_
# prefix to be accessible in the browser via import.meta.env
#
# SECURITY NOTE: Never commit your .env file to version control.
# The .env file is already in .gitignore for your protection.
# ============================================================

# ------------------------------------------------------------
# Supabase Configuration (Required)
# ------------------------------------------------------------
# These values are required for the application to connect
# to your Supabase backend. Find them in your Supabase project
# dashboard under Project Settings > API.

# Your Supabase project identifier
# Location: Project Settings > General > Reference ID
# Example: "udqfdeoullsxttqguupz"
VITE_SUPABASE_PROJECT_ID="your-project-id"

# Supabase public API key (anon key)
# Location: Project Settings > API > Project API keys > anon public
# Note: This key is safe for client-side use but should still not
# be committed to version control with actual values.
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key-here"

# Supabase project endpoint URL
# Location: Project Settings > API > Project URL
# Format: https://<project-id>.supabase.co
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# ------------------------------------------------------------
# Render API Configuration (Required for script generation)
# ------------------------------------------------------------
# Render API endpoint for long-running script generation
# Render has no timeout limits, enabling 20k-30k word scripts
# Location: Render Dashboard > Your Service > URL at top
# Format: https://your-service.onrender.com
VITE_RENDER_API_URL="https://history-gen-ai.onrender.com"
