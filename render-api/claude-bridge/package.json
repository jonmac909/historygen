{
  "name": "claude-bridge",
  "version": "0.1.0",
  "description": "Local HTTP bridge that routes Anthropic SDK-shape requests through Claude Code CLI (subscription billing).",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "express": "^4.19.2",
    "lru-cache": "^10.4.3",
    "p-limit": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
